{
	"server": {
		"host": "0.0.0.0",
		"port": 8000 // if this port is not available, next available one will be picked automatically
	},
	"folders": {
		"data_root": "./data", // the root directory where all internal data will be saved
		"sites": "./data/sites", // all websites will be stored here (in their original source)
		"public_html": "./data/public_html" // the generated static websites go here
	}
}